{
  "$schema": "https://schema.management.azure.com/schemas/2015-01-01/deploymentParameters.json#",
  "contentVersion": "1.0.0.0",
  "parameters": {
    "web_app_name": {
      "value": "{{SET FROM PIPELINE}}"
    },
    "static_web_app_name": {
      "value": "{{SET FROM PIPELINE}}"
    },
    "server_farm_name": {
      "value": "{{SET FROM PIPELINE}}"
    },
    "database_server_name": {
      "value": "{{SET FROM PIPELINE}}"
    },
    "server_farm_location": {
      "value": "{{SET FROM PIPELINE}}"
    },
    "static_web_app_location": {
      "value": "{{SET FROM PIPELINE}}"
    }
  }
}
